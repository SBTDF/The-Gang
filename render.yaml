services:
  - type: web
    name: the-gang
    env: node
    plan: free
    branch: main
    buildCommand: NPM_CONFIG_PRODUCTION=false npm install && npm run build --workspace client
    startCommand: npm start
    envVars:
      - key: CLIENT_URL
        value: "*"
    autoDeploy: true
